[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "calibrated-explanations-explanation"
version = "0.2.0"
description = "Curated explanation plugin metapackage for calibrated-explanations"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    # Curation policy: only plugins with status = "mature" may be listed here.
    # No plugin has completed a maturity review yet, so this set is empty.
]

[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ce_plugin_repo]
family = "meta"
