[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "calibrated-explanations-plugins"
version = "0.1.0"
description = "Official metapackage for calibrated-explanations plugins"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "calibrated-explanations-calibration>=0.1,<1",
    "calibrated-explanations-explanation>=0.1,<1",
    "calibrated-explanations-visualization>=0.1,<1"
]

[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ce_plugin_repo]
family = "meta"
