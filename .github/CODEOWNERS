# Single-maintainer repository: one rule until a second maintainer or team
# exists. Note: code-owner review is only independently enforced once branch
# protection requires it AND an eligible independent reviewer exists; today
# the protected `pypi` environment is the final publication approval boundary.
* @tuvelofstrom
